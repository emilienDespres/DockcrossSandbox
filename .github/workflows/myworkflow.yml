name: Dockross Action Sandbox
on:
  workflow_dispatch:
  push:

jobs:
  Sahara:
    runs-on: windows-2016

    defaults:
      run:
        shell: bash

    steps:
      - name: checkout code
        uses: actions/checkout@v2


      - name: docker init
        run: |
          dockerpull dockcross/windows-shared-x86

        # docker build -f docker/Dockerfile -t dockercross .