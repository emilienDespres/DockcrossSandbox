name: Dockross Action Sandbox
on:
  workflow_dispatch:
  push:

jobs:
  Sahara:
    runs-on: windows-2016

    steps:
      - name: docker init
        run: docker build -f docker/Dockerfile -t dockercross.